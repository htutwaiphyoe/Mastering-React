import React from "react";
import Counter from "./container/Counter/Counter";
const App = (props) => {
    return (
        <div>
            <Counter />
        </div>
    );
};

export default App;
